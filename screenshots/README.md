# Ảnh Chụp Bản Deploy

- `dashboard.png`: Railway production dashboard, agent và Redis đều Online,
  deployment đang Active và Successful.
- `health.png`: kết quả gọi Public URL `/health` và `/ready`, xác nhận service
  hoạt động và kết nối Redis thành công.
